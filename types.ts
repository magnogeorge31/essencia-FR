export interface FaqItem {
  question: string;
  answer: string;
}

export interface BonusItem {
  number: number;
  title: string;
  description: string;
  image?: string;
}

export interface ModuleItem {
  title: string;
  image: string;
}